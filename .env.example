# Supabase-проект First Logic. Оба значения публичны по дизайну Supabase:
# защита данных — RLS-политики в БД (supabase/migrations).
# Для GitHub Pages задаются как Repository variables, не secrets.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
